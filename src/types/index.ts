export interface Video {
  id: number;
  title: string;
  thumbnail: string;
  duration: string;
  views: string;
  uploadedAt: string;
  author: string;
  verified: boolean;
  description?: string;
}